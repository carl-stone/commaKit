---
type: Project Snapshot
title: commaKit Current Project Status
description: Current package metadata, source/test inventory, and active context for future agents.
resource: DESCRIPTION
tags: [status, package-metadata, tests, source-inventory]
timestamp: 2026-06-15T00:00:00Z
status: current
owner: Carl Stone
---

# Package Metadata

| Field | Current value |
|---|---|
| Package | `commaKit` |
| Title | Comparative Microbial Methylomics Analysis Kit |
| Version | `0.2.0` |
| R | `>= 4.3.0` |
| CI target | R 4.5 |
| License | MIT |
| GitHub | `carl-stone/commaKit` |

# Current Milestones

- Schema v2 is complete and merged as the `0.2.0` baseline.
- Technical rename to `commaKit` is complete.
- Test quality work is still in progress; older audit cards are useful but not a complete current inventory.
- Plot tests are still a known place to strengthen behavioral contracts.
- Layered assays and named differential-methylation result layers are present in the codebase.

# Source Inventory

Primary implementation directories:

```text
R/
tests/testthat/
vignettes/
data-raw/
inst/
man/
```

Important current R files:

```text
commaData_class.R
commaData_constructor.R
accessors.R
assay_layers.R
result_layers.R
annotateSites.R
diffMethyl.R
coverage_analysis.R
summarizeRegions.R
```

# Test Inventory

`tests/testthat/` now includes helper fixtures and tests for assay layers, result layers, site filters, DelayedArray behavior, identity docs, project metadata, vignettes, and `summarizeRegions()`. If a table in [Test Quality Audit](test-quality.md) does not mention these newer files, treat that table as historical audit evidence rather than a current inventory.

Use this command for the live list:

```bash
rg --files tests/testthat | sort
```

# Example Data

Synthetic data is generated by `data-raw/create_example_data.R` with deterministic seed `1312`.

Current durable facts:

- 588 sites total
- 393 6mA/GATC sites
- 195 5mC/CCWGG sites
- 6 samples: `ctrl_1` through `ctrl_3`, `treat_1` through `treat_3`
- Genome: `chr_sim`, 100 kb
- Ground truth: 30 of 393 6mA sites are differentially methylated

# Current Gotchas

- `mod_context` uses underscores, for example `6mA_GATC`. Some stale prose may still show colon examples; prefer code and this card.
- Use `siteCoverage()` rather than `coverage()` in new code and docs.
- `condition` is optional container metadata but required by workflows that need a grouping/design variable.
- `man/*.Rd` files are generated from roxygen and must not be hand-edited.

