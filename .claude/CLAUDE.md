# commaKit Claude Guide

Claude should use the shared agent directives for this repository.

## Required Reading

1. Read the repository root `AGENTS.md` first.
2. Read the nearest `AGENTS.md` in every directory you touch.
3. Use `dev/knowledge/index.md` for durable project knowledge.
4. Use `dev/knowledge/architecture.md` for package architecture and API contracts.
5. Use `dev/knowledge/agent-harness.md` for agent operating expectations.

## Local Rule

Do not duplicate package facts here. If a durable fact changes, update the shared `AGENTS.md` hierarchy or the relevant `dev/knowledge/` card.
